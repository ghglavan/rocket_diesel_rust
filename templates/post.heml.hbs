<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Rocket: Sessions</title>
</head>

<body>

    {{#if error}}
    <p>Error: {{error}}</p>
    {{/if}}

    {{#if author}}
    <p>
        {{author}} : {{#if title}}{{title}}{{/if}} at {{#if date}}{{date}}{{/if}}
    </p>

    <p>
        {{#if body}}{{body}}{{/if}}
    </p>
    {{/if}}


    <a href="/">back</a>
</body>

</html>